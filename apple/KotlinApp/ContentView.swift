import SwiftUI
import KotlinAppShared

struct ContentView: View {
    var body: some View {
        Button("Test") {
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
